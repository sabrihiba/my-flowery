<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Flowery{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    {% block stylesheets %}
    <style>
        #searchBarContainer {
            position: fixed;
            top: -100%;
            left: 0;
            width: 100%;
            background-color: white;
            transition: top 0.3s ease;
            padding: 20px;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-right: 50px;
        }

        #searchBarContainer.active {
            top: 0;
        }

        #searchBar {
            width: 85%;
            padding: 10px;
        }

        #searchButton {
            font-size: 20px;
            cursor: pointer;
            background-color: transparent;
            border: none;
            color: rgb(224, 163, 204);
        }

        #closeSearch {
            font-size: 30px;
            cursor: pointer;
            position: absolute;
            right: 20px;
            top: 20px;
        }

        #searchBarContainer button {
            margin-left: 10px;
        }

        #searchBar:focus {
            outline: none;
            box-shadow: none;
            border-color: rgb(224, 163, 204);
        }

        footer {
            background-color: #f8f9fa;
            padding: 20px 0;
            text-align: center;
        }

        footer .social-icons a {
            margin: 0 10px;
            color: #333;
            text-decoration: none;
        }

        footer .social-icons a:hover {
            color: #e43f7f;
        }

        footer .links a {
            color: #333;
            text-decoration: none;
        }

        footer .links a:hover {
            color: #e43f7f;
        }
    </style>
    {% endblock %}
</head>
<body>
    <header class="bg-light border-bottom">
        <div class="container">
            <div class="d-flex align-items-center justify-content-between py-3">
                <div>
                    <a href="/profile" class="text-decoration-none text-dark">
                        <i class="bi bi-person-circle fs-4"></i>
                    </a>
                </div>

                <div class="text-center">
                    <img src="{{ asset('images/logoo.jpg') }}" alt="Flower Logo" style="width: 40px; height: 40px;">
                    <h1 class="d-inline fs-4 ms-2">Welcome to our Flowery</h1>
                </div>

                <div class="d-flex align-items-center">
                    <i class="bi bi-search fs-4 me-3" id="searchIcon" style="cursor: pointer;"></i>
                    <a href="/cart" class="text-decoration-none text-dark">
                        <i class="bi bi-cart fs-4"></i>
                    </a>
                </div>
            </div>

            <nav class="mt-3">
                <ul class="nav justify-content-center">
                    <li class="nav-item"><a href="/home" class="nav-link text-dark">Home</a></li>
                    <li class="nav-item"><a href="/shop" class="nav-link text-dark">Shop</a></li>
                    <li class="nav-item"><a href="/contact" class="nav-link text-dark">Contact Us</a></li>
                    <li class="nav-item"><a href="/aboutus" class="nav-link text-dark">About Us</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <div id="searchBarContainer">
        <input type="text" id="searchBar" class="form-control" placeholder="What are you looking for?">
        <button type="submit" class="btn btn-primary" id="searchButton">
            <i class="bi bi-search"></i>
        </button>
        <i class="bi bi-x-circle fs-3" id="closeSearch"></i>
    </div>

    {% block body %}{% endblock %}

    <footer>
        <div class="container">
            <div class="social-icons mb-3">
                <a href="#" class="bi bi-facebook fs-3"></a>
                <a href="#" class="bi bi-twitter fs-3"></a>
                <a href="#" class="bi bi-instagram fs-3"></a>
                <a href="#" class="bi bi-linkedin fs-3"></a>
            </div>
            <div class="links">
                <a href="#" class="me-3">Privacy Policy</a>
                <a href="#">Terms of Service</a>
            </div>
            <p class="mt-3">&copy; 2025 Flowery. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}
    <script>
        document.getElementById('searchIcon').addEventListener('click', function () {
            document.getElementById('searchBarContainer').classList.add('active');
        });

        document.getElementById('closeSearch').addEventListener('click', function () {
            document.getElementById('searchBarContainer').classList.remove('active');
        });
    </script>
    {% endblock %}
</body>
</html>
